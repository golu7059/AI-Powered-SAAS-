export interface Video{
    id: string,
    public_id: string,
    title: string,
    description: string,
    publicId : string,
    originalSize: string,
    compressedSize: string,
    duration:number,
    createdAt: string
}